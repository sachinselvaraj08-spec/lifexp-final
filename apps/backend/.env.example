PORT=5000

# Firebase service account credential path or inline JSON
# You can place the download file as service-account.json in backend/src/config/
FIREBASE_SERVICE_ACCOUNT_PATH=./src/config/service-account.json
